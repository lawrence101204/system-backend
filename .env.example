PORT=5000
NODE_ENV=development

# Comma-separated allowed origins (for teacher demo you can allow localhost)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# XAMPP defaults usually work with empty password
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lavera

JWT_SECRET=super_secret_key_change_me
JWT_EXPIRES_IN=1d
BCRYPT_ROUNDS=12

LOGIN_RATE_WINDOW_MIN=15
LOGIN_RATE_MAX=10

# Email optional (leave blank if not using)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_TO=
